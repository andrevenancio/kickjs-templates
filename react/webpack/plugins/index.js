const clean = require('./clean');
const copy = require('./copy');
const core = require('./core');
const hmp = require('./hmp');
const html = require('./html');
const nmp = require('./nmp');
const uglify = require('./uglify');
const css = require('./css');

module.exports = {
    clean,
    copy,
    core,
    hmp,
    html,
    nmp,
    uglify,
    css,
};
